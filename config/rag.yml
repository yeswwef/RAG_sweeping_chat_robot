chat_model_name: qwen3-max
embedding_model_name: text-embedding-v4


hybrid_retrieval:
  enabled: true
  dense_top_n: 10 # 稠密路候选数
  sparse_top_n: 10 # 稀疏路候选数
  rrf_k: 10 # RRF 常数

query_rewrite:
  enabled: true
  max_queries: 3 # 原问题 + 改写 + 1 个子查询