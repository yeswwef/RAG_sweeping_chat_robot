# 🤖 agent扫地机器人 · 工程化改造清单

> 改造时间：2026-07-31
> 改造依据：《AI Agent 项目改造 - 手把手教程》（第1步工程化 / 第2步 FastAPI / 第3步 Docker）

---

## 一、改造前 vs 改造后

| 维度 | 原来 | 现在 |
|------|------|------|
| 使用方式 | 只能在 PyCharm 里点运行，本地聊天 | 可安装的标准工程 + 对外 HTTP 服务 + 可 Docker 部署 |
| 项目文件 | 只有代码文件夹，无任何工程文件 | pyproject.toml / requirements / .env / .gitignore 齐全 |
| API Key | 依赖系统环境变量 | 统一放 `.env`，代码统一读取 |
| 对外接口 | 无 | FastAPI 提供 `/health` + `/chat`，自带 `/docs` 文档 |
| 部署 | 无法部署 | Dockerfile + docker-compose 一键打包启动 |
| RAG 代码 | `vector_store.py` 有 import bug，一启动就崩 | 已修复，可正常导入运行 |
| 虚拟环境 | venv 里连 pip 都没有 | pip 已装，100+ 依赖安装完成 |

---

## 二、新增文件（原来没有的）

| 文件 | 作用 | 为什么要加 |
|------|------|-----------|
| `pyproject.toml` | 项目元数据 + 依赖声明 + 打包配置 | Python 工程化的标准入口，`pip install -e .` 即可安装项目；声明版本、依赖、包范围 |
| `requirements.txt` | 依赖清单（一行一个包） | Docker 构建、新环境复制时用 `pip install -r requirements.txt`，比 pyproject 更简单直接 |
| `.env` | 存放 API Key 等敏感配置 | 密钥不进代码、不进 Git；换 Key 只改这一个文件，不用动代码 |
| `.env.example` | .env 的格式模板 | 提交到 Git 给其他人参考格式，又不泄露真实 Key |
| `.gitignore` | 忽略 .env、__pycache__、chroma_db、logs 等 | 防止密钥泄露、防止临时文件/数据库/虚拟环境进版本库 |
| `.dockerignore` | 构建镜像时忽略的文件清单 | 让镜像只包含运行必需内容，减小体积、加速构建 |
| `Dockerfile` | 多阶段构建：Python 3.12 + 依赖 + 代码打包成镜像 | 把整个运行环境装进"集装箱"，任何机器一行命令启动，不用手动装环境 |
| `docker-compose.yml` | 编排 agent-api + Redis 两个服务 | 一条命令启动整套服务；端口映射、数据卷持久化（chroma_db/data/logs 不丢）、挂了自动重启 |
| `utils/env.py` | Settings 配置类：集中管理所有配置项 | 任何文件 `from utils.env import settings` 就能读配置，改配置只改一处 |
| `api/__init__.py` + `api/server.py` | FastAPI 服务：`/health` 健康检查 + `/chat` 对话接口 | 把 Agent 变成标准 HTTP 服务，网页/手机App/其他程序都能调用；自动生成接口文档 |

---

## 三、修改优化（原来就有的文件）

| 文件 | 原来 | 改成了什么 | 作用 |
|------|------|-----------|------|
| `model/factory.py` | 直接依赖系统环境变量里的 Key | 顶部加 `os.environ.setdefault("DASHSCOPE_API_KEY", settings.dashscope_api_key)` | 启动时自动把 `.env` 的 Key 注入；用 `setdefault` 不会覆盖系统已有的 Key（比教程的直接赋值更安全） |
| `rag/vector_store.py` | `from document import Document`（模块不存在，一跑就崩） | `from langchain_core.documents import Document` | 修复致命 bug，RAG 知识库加载代码能正常运行 |
| `rag/rag_service.py` | `from docx.document import Document`（误用，只是类型注解） | `from langchain_core.documents import Document` | 修正错误导入，不再依赖多余 python-docx 包 |
| `utils/env.py`（首版） | `load_dotenv()` 依赖当前工作目录 | 按文件路径定位项目根目录的 `.env` | 从任何目录启动都能加载配置，不再受"必须在哪个目录运行"限制 |

---

## 四、环境与验证（结果）

| 项目 | 结果 |
|------|------|
| venv 补装 pip | ✅ pip 25.3 |
| 依赖安装 `pip install -e .` | ✅ 100+ 包（langchain 1.3.14 / langgraph 1.2.10 / chromadb 1.5.9 / fastapi 0.141.1 / dashscope 1.26.4 / streamlit 1.60.0） |
| `import api.server` | ✅ 成功 |
| uvicorn 启动 + `/health` | ✅ 返回 200 `{"status":"ok"}` |
| `.env` 读取 | ✅ 已读到真实 Key（你已填写） |
| Streamlit 界面 | ✅ 可直接运行 |

---

## 五、顺手修掉的教程本身的坑

| 坑 | 说明 | 修法 |
|----|------|------|
| build-backend 写错 | 教程写 `setuptools.backends._legacy:_Backend`，新环境 import 不到会直接报错 | 改成标准 `setuptools.build_meta:__legacy__` |
| 打包范围未声明 | 项目有 9 个顶层包（agent/api/rag/model/utils/config/data/logs/prompts），setuptools 自动发现会报 "Multiple top-level packages" | pyproject.toml 加 `[tool.setuptools.packages.find]` 显式声明 |

---

## 六、没动的部分（核心代码保持原样）

`agent/`（Agent 逻辑）、`rag/`（知识库，仅修导入）、`utils/`（仅新增 env.py）、`config/`、`data/`、`prompts/`、`logs/`、`app.py`（Streamlit 界面）——业务逻辑零改动，只加了工程外壳和修了两处 import bug。
