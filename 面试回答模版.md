# 扫地机器人 AI 客服 Agent —— 面试回答模版

> 项目定位：一个面向「扫地机器人 / 扫拖一体机器人」的知识问答客服 Agent。
> 核心能力：ReAct 工具调用（Agent）+ RAG 混合检索（知识库问答）+ 多轮记忆 + 个性化使用报告 + 联网天气适配。

---

## 0. 一句话自我介绍（电梯陈述）

「我做过一个扫地机器人智能客服 Agent。它基于 LangGraph 的 ReAct 架构，把大模型 + 工具调用 + RAG 检索串起来：
用户问『扫地机器人怎么保养』，Agent 先判断需不需要查资料，需要的话调 RAG 工具，走『查询改写 → 稠密向量 + BM25 稀疏检索 → RRF 融合 → LLM 总结』的链路给出答案；
用户要『生成我的使用报告』，Agent 会按固定流程依次调工具拿到用户 ID、月份、外部系统使用记录，并通过中间件动态切换成报告提示词生成 Markdown 报告；
还支持联网查天气、多轮记忆、会话管理。整体做了 FastAPI + Streamlit 双入口、SQLite 持久化、Docker 部署，并搭了一套检索/生成/延迟的量化评测体系。」

---

## 1. 整体架构（面试官问：介绍下项目结构）

回答要点：**分层 + 单一职责**，不是把所有逻辑写在一个文件里。

```
agent扫地机器人/
├── app.py                 # Streamlit 前端入口（聊天 UI + 会话侧边栏）
├── api/server.py          # FastAPI 后端（HTTP/SSE 接口 + 鉴权 + 限流）
├── agent/
│   ├── react_agent.py     # ReAct Agent 核心（LangGraph create_agent）
│   └── tools/             # 工具定义 + 中间件
├── rag/                   # RAG：向量库 / 混合检索 / 查询改写 / 总结服务
├── model/factory.py       # 模型工厂（LLM + Embedding）
├── prompts/               # 提示词（主/总结/改写/报告）
├── config/*.yml           # rag/chroma/prompts/agent 配置
├── data/                  # 知识库语料 + 外部数据 records.csv + conversations.db
├── utils/                 # DB / 配置 / 环境 / 日志 / 文件 / 路径
├── eval/                  # 评测体系（检索/生成/延迟 + 黄金集）
└── test/                  # pytest 单元/集成测试
```

数据流（一条问答的完整链路）：

```
用户输入
  → Streamlit(app.py) 或 FastAPI(api/server.py)
  → ReactAgent.execute_stream(session_id)
  → LangGraph Agent（ReAct 循环：思考→行动→观察）
      ├─ 调 rag_summarize 工具 → RAG 链路（改写→混合检索→生成）
      ├─ 调 get_weather 等工具 → 联网/外部数据
      └─ 调 fill_context_for_report → 中间件动态切换报告提示词
  → 流式吐回答案 → 写入 SQLite（conversations / messages）
```

---

## 2. 为什么用 LangGraph / ReAct（面试官问：Agent 怎么设计的）

回答要点：**ReAct = 思考(Thought) + 行动(Action) + 观察(Observation) 循环**。

- 用 `langchain.agents.create_agent` 创建，绑定 `model`、`system_prompt`、`tools`、`middleware`、`checkpointer`。
- 系统提示词里明确写了「严格遵循 思考→行动→观察→再思考 的流程」，并规定：信息不足就调工具，工具返回后再判断够不够，5 次工具调用后仍不足就回「我不知道」。
- **ReAct 相比直接 prompt 的好处**：模型不是一次性生成，而是可以主动决定「要不要查、查什么、查完再想」，适合客服这种需要「外挂知识/外部数据」的场景，也能减少幻觉、可追踪每一步决策。
- 通过 `middleware` 做横切能力（日志、工具监控、动态提示词），不侵入业务逻辑。

---

## 3. 中间件 Middleware（面试官问：中间件干嘛用的）

回答要点：用 LangChain/LangGraph 的 middleware 做**横切关注点**。

- `monitor_tool`（`@wrap_tool_call`）：记录每个工具名/入参/是否成功；并在调用 `fill_context_for_report` 时把 `runtime.context["report"] = True` 写进运行时上下文。
- `log_before_model`（`@before_model`）：模型调用前打印消息条数和最后一条消息，方便排查。
- `report_prompt_switch`（`@dynamic_prompt`）：根据 `context["report"]` 动态决定用「普通客服提示词」还是「报告写手提示词」，实现**同一 Agent 两种行为模式**。

亮点：把「报告场景」的实现方式从「模型自己硬背流程」升级为「工具 + 中间件状态机」，更可控、可测试。

---

## 4. 多轮记忆 / 会话隔离（面试官问：怎么记住上下文、怎么隔离会话）

回答要点：**Checkpointer + thread_id**。

- `ReactAgent` 里用 `langgraph-checkpoint-sqlite` 的 `SqliteSaver` 做持久化 checkpointer。
- `execute_stream(query, session_id)` 把 `config={"configurable": {"thread_id": session_id}}` 传给 `agent.stream`，同一 `thread_id` 自动带出历史状态，不同 `thread_id` 互不干扰。
- 前端/API 的会话 uuid 与 LangGraph 的 thread_id **共用同一个 uuid**，所以 UI 会话列表和 Agent 记忆天然对齐。
- 删除会话时，除了删数据库，还会调 `agent.delete_thread(thread_id)` 清理记忆线程，避免残留。

常被追问的细节：checkpoint 持久化到 SQLite，服务重启后按 thread_id 还能找回历史；这就是「持久化记忆」而不是只存在内存里。

---

## 5. RAG 完整链路（面试官问：RAG 是怎么做的）

回答要点：把它拆成**离线入库 + 在线检索 + 生成**三段讲。

**离线入库（vector_store.py）**
1. 读取 `data/` 下的 txt / pdf（`TextLoader` / `PyPDFLoader`）。
2. `RecursiveCharacterTextSplitter` 按 `chunk_size=200, chunk_overlap=20` 分块，分隔符同时覆盖中英文标点。
3. 用 `DashScopeEmbeddings(text-embedding-v4)` 向量化，写入 ChromaDB（`persist_directory` 持久化）。
4. 增量入库：给每个文件算 **MD5**，已存在的跳过，只灌新文件（`md5.text` 存指纹）。

**在线检索（hybrid_retriever.py + query_rewriter.py）**
1. 查询改写：LLM 生成「改写查询 + 子查询」，多路召回提升 recall。
2. 双路召回：稠密（向量相似度）+ 稀疏（BM25 关键词）。
3. RRF 融合排序去重。

**生成（rag_service.py）**
- 把检索到的 chunk 拼成 `[1]: 内容 | metadata` 格式放进 prompt，LLM 基于参考资料总结，prompt 里约束「不得编造、只基于资料回答」。
- 用 `PromptTemplate | model | StrOutputParser()` 的 LCEL 链式写法。

---

## 6. 混合检索 + RRF（面试官问：为什么混合检索，RRF 是什么）

回答要点：**稠密抓语义，稀疏抓精确关键词，RRF 融合排序**。

- 稠密路：`Chroma.similarity_search`，语义相近但可能漏掉精确词（如型号、专有名词）。
- 稀疏路：`rank_bm25.BM25Okapi`，中文用 `jieba` 分词后做关键词精确匹配，能兜住「口语化/型号词」。
- **RRF（Reciprocal Rank Fusion）**：`score(d) = Σ 1/(k + rank(d))`，不依赖两路的分数量纲（向量相似度和 BM25 分数不能直接相加），只按排名融合，天然去重、公平。
- 去重对齐：对 chunk 正文算 `md5` 作为唯一 key，两路拿到同一文档时能合并。
- 性能优化：BM25 索引**版本化懒构建**——用「语料 count + id 指纹」判断是否变化，进程内只建一次，还加了线程锁防并发重复建；空语料时不建索引，避免除零。

---

## 7. 查询改写（面试官问：查询改写解决什么问题）

回答要点：**解决口语化、指代、召回不足**。

- 用户问「我家 50 平买哪个好」这类口语化、有指代的问题，直接向量检索效果差。
- `QueryRewriter` 用 LLM 把原问题变成「改写后更完整的问题 + 多个侧面子查询」，再分别检索，最后 RRF 融合，提升 recall。
- 工程细节：LLM 输出不一定干净，`_parse` 做了**容错解析**（整体 JSON → 正则提取 `[...]` → 按行兜底）；异常或开关关闭时回退原查询，保证可用性。

---

## 8. 工具（Tools）设计（面试官问：有哪些工具）

回答要点：工具 = Agent 的「手」，能力边界在系统提示词里写清楚。

- `rag_summarize(query)`：RAG 检索总结，核心知识工具。
- `get_weather(city)`：联网查 Open-Meteo 实时天气 + 未来三天，用于「天气/湿度是否适合机器人使用」类问题。
- `get_user_location()` / `get_user_id()` / `get_current_month()`：用户上下文（当前是 mock/随机，演示用）。
- `fetch_external_data(user_id, month)`：从 `data/external/records.csv` 读指定用户某月的使用记录。
- `fill_context_for_report()`：报告场景的**前置触发工具**，本身无业务返回，只触发中间件切换提示词。

亮点（报告强约束）：主提示词里硬性要求「生成报告必须 先 get_user_id → get_current_month → fill_context_for_report → fetch_external_data」，且未调 `fill_context_for_report` 禁止后续报告操作——把业务流程固化进 prompt + 工具 + 中间件。

---

## 9. 模型工厂模式（面试官问：模型怎么抽象）

回答要点：**抽象工厂 + 单例**，方便切换模型。

- `BaseModelFactory`（ABC）定义 `generator()` 抽象方法。
- `ChatModelFactory` 生成 `ChatTongyi(qwen3-max)`，`EmbeddingsFactory` 生成 `DashScopeEmbeddings(text-embedding-v4)`。
- 模块加载时直接生成全局单例 `chat_model` / `embedding_model`，业务代码只 import 单例，不关心底层实现。
- Key 通过 `.env` 读入并用 `os.environ.setdefault` 注入，不覆盖系统已有环境变量。

---

## 10. 数据持久化（面试官问：会话和消息怎么存）

回答要点：**SQLite + WAL + 外键级联**。

- 两张表：`conversations(id uuid, title, created_at, updated_at)`、`messages(conversation_id 外键, role, content, ...)`。
- `PRAGMA foreign_keys = ON` 删除会话时**级联删除**消息。
- `PRAGMA journal_mode = WAL`：读写不互斥，适合 Streamlit 和 FastAPI 两个进程共享同一个 db；`busy_timeout` 避免并发写锁。
- 懒创建会话：用户第一次提问才真正建会话（空会话不入库）；第一句问题自动截前 20 字做标题。
- `get_conn()` 全局单例 + `check_same_thread=False`，SQLite 连接复用。

---

## 11. API 设计（面试官问：后端接口怎么设计的）

回答要点：**FastAPI + SSE 流式 + 鉴权 + 限流**。

- `GET /health`：健康检查，顺带 `SELECT 1` 验证数据库连通性。
- `POST /chat`：`stream=false` 返回 JSON，`stream=true` 返回 SSE 流式（`text/event-stream`）。
- `GET /conversations`、`DELETE /conversations/{id}`：会话管理，删除时同步清 LangGraph 记忆线程。
- SSE：用 `StreamingResponse` + `iterate_in_threadpool` 把同步生成器放到线程池，避免阻塞事件循环；`finally` 里把已生成内容落库，流中断也不丢记录。
- 鉴权：可选 `X-API-Key`，用 `hmac.compare_digest` 防时序攻击；未配置 key 时跳过（本地开发）。
- 限流：内存滑动窗口 `RateLimiter`，默认每 IP 30 次/分钟。
- 请求/响应用 Pydantic 模型校验（`query` 长度 1–2000）。

---

## 12. 配置与工程化（面试官问：配置、日志、部署怎么做）

回答要点：

- **配置分层**：YAML 管业务参数（rag/chroma/prompts/agent），`.env` 管密钥（python-dotenv），`Settings` 类集中管理。
- **日志**：`logging` 双 handler（控制台 INFO + 文件 DEBUG），按天分文件，`get_logger` 幂等（已有 handler 不再重复加）。
- **路径**：`get_abs_path` 基于 `__file__` 定位项目根，不依赖启动时工作目录。
- **Docker**：Dockerfile 多阶段构建（builder 装依赖 + runtime 拷 site-packages），国内镜像源；docker-compose 起 `agent-api`(8000) + `agent-ui`(8501) 两个服务，卷挂载持久化 chroma_db / data / logs，带 healthcheck。

---

## 13. 评测体系（面试官问：怎么验证效果好、怎么评测 RAG）

回答要点：把链路拆成**检索、生成、延迟**三段分别量化。

**检索指标（metrics.py）**
- `hit@k`（有没有命中）、`recall@k`（找回多少）、`mrr@k`（第一个相关排第几）、`ndcg@k`（排序质量）、`context_precision@k`（相关 chunk 是否排前、无关是否稀释上下文）、`recall_full`（整批召回）。

**四基线对比（run_retrieval_eval.py）**
- 纯稠密 / 纯 BM25 / 混合 RRF / 混合+改写，量化每项改造的收益。

**生成指标（run_generation_eval.py + judge.py）**
- LLM-as-Judge，用独立的 `qwen-plus` 裁判（与被测 `qwen3-max` 分离），评 faithfulness（忠实度/幻觉）、relevance（相关性）、completeness（完整度）。

**延迟/成本（run_latency_eval.py）**
- 分阶段计时：改写 → 稠密 → 稀疏+融合 → 生成，统计 P50/P95，并从 token usage 估算成本。

**黄金集（gold.py + validate_gold.py + build_eval_set.py）**
- 从语料自动解析 QA/故障/知识条目，答案通过「最长前缀匹配」映射到相关 chunk（用 md5 作稳定键）；还生成同义/口语化变体，检验「换个说法还能不能找到」；`validate_gold.py` 输出覆盖率、空 gold、重复率做质量校验。

---

## 14. 项目亮点 / 你可以主动讲的加分点

1. **完整闭环**：从 Agent、RAG、工具、记忆、持久化、API、前端到评测、部署一条龙，不是 demo。
2. **工程细节扎实**：MD5 增量入库、BM25 版本化懒构建 + 锁、查询改写容错解析、RRF 去重对齐、SSE 断流落库、外键级联 + WAL。
3. **可观测/可评测**：middleware 日志 + 三层评测（检索/生成/延迟）。
4. **业务场景真实**：报告生成强约束流程、联网天气适配、个性化用户报告，体现「把业务规则工程化」。

---

## 15. 可能的追问与回答方向

- **Q：RRF 和直接加权平均有什么区别？** A：两路分数量纲不同不能直接加，RRF 只看排名，更鲁棒、天然去重。
- **Q：为什么用 BM25 而不是纯向量？** A：中文专有名词/型号、精确关键词，向量召回会漏，BM25 关键词命中更稳；混合互补。
- **Q：chunk 大小怎么定的？** A：200 字 + 20 重叠，平衡语义完整性和粒度；分隔符覆盖中英文标点。
- **Q：怎么防幻觉？** A：RAG 提示词强约束「只基于参考资料、不编造」+ faithfulness 评测 + Agent 工具返回不足时回「我不知道」。
- **Q：多进程部署时单例/内存会有什么问题？** A：agent 单例按进程复制，多 worker 各持一份；限流、外部数据缓存是进程内内存态，多实例需换 Redis 等共享存储——这是可说的演进方向。

