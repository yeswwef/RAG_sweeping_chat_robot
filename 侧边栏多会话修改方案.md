# 侧边栏多会话管理 · 修改方案文档

> 日期：2026-08-13
> 状态：**待审批**（此文档仅描述方案，未改动任何代码）

---

## 一、需求确认

在 Streamlit 前端（`app.py`）左侧增加一个**会话侧边栏**，功能：

1. ✅ 展示所有历史对话记录（标题 + 时间）
2. ✅ 点击某条记录 → 跳转到该对话，**继续聊天**（上下文不丢）
3. ✅ 「新建对话」按钮 → 开启全新会话（新 uuid）
4. ✅ 每条记录有「删除」按钮 → 删除该对话
5. ✅ 每个对话有独立 uuid，历史互不干扰
6. ✅ 对话记录**长期存在**（重启服务、刷新页面都不丢）

---

## 二、现状分析（为什么现在做不到）

| 问题 | 现状 | 后果 |
|------|------|------|
| Agent 记忆 | `ReactAgent` 用 `MemorySaver()`（**纯内存**） | 服务一重启，所有对话记忆全部清空，无法"长期存在" |
| 前端消息 | `st.session_state["message"]`（**页面内存**） | 刷新页面消息就没了，切换会话更无从谈起 |
| 会话索引 | 只有当前一个 `session_id`，无会话列表 | 无法展示/切换/删除历史对话 |
| 消息存储 | 无数据库 | 历史记录无处存放 |

**结论：要实现"长期存在 + 多会话切换"，必须引入持久化存储（SQLite）并改造 Agent 的 checkpointer。**

---

## 三、总体方案

### 架构

```
Streamlit 侧边栏（会话列表 UI）
        │ 点击/新建/删除
        ▼
┌────────────────────────────┐
│   SQLite 数据库            │  ← 新增：conversations.db
│  ┌──────────────────────┐  │
│  │ conversations 表     │  │  会话索引：uuid、标题、创建/更新时间
│  ├──────────────────────┤  │
│  │ messages 表          │  │  消息：会话 uuid、角色、内容、时间
│  └──────────────────────┘  │
└────────────────────────────┘
        │ thread_id = 会话 uuid
        ▼
┌────────────────────────────┐
│   Agent（LangGraph）       │
│   MemorySaver → SqliteSaver │  ← 改：记忆也持久化
└────────────────────────────┘
```

**双持久化原则：**
- `messages` 表 → 前端展示用的聊天记录（谁说了什么）
- Agent `SqliteSaver` → 模型侧的对话状态（LangGraph 内部记忆）

两者都按 `session_id`（= uuid）关联，切换会话时同时加载，保证"继续聊天"上下文完整。

---

## 四、文件改动清单

| 文件 | 操作 | 改动内容 |
|------|------|----------|
| `utils/db.py` | 🆕 新建 | SQLite 封装：会话增删查、消息存取 |
| `app.py` | ✏️ 改造 | 侧边栏 UI + 会话切换逻辑 + 消息持久化 |
| `agent/react_agent.py` | ✏️ 小改 | `MemorySaver()` → `SqliteSaver` |
| `requirements.txt` | ✏️ 追加 | `langgraph-checkpoint-sqlite` |
| `docker-compose.yml` | ✏️ 追加 | 挂载 `conversations.db` 卷，防止容器重启丢数据 |
| `.gitignore` | ✏️ 追加 | 忽略 `*.db` |
| `test/test_session.py` | 🆕 新建 | 会话 CRUD 单元测试 |

---

## 五、核心设计细节

### 5.1 数据表结构（utils/db.py）

```sql
-- 会话表
CREATE TABLE IF NOT EXISTS conversations (
    id          TEXT PRIMARY KEY,          -- uuid4 字符串
    title       TEXT NOT NULL DEFAULT '新对话',  -- 侧边栏显示名
    created_at  TEXT NOT NULL,             -- ISO 时间
    updated_at  TEXT NOT NULL
);

-- 消息表
CREATE TABLE IF NOT EXISTS messages (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    conversation_id TEXT NOT NULL,         -- 关联 conversations.id
    role           TEXT NOT NULL,          -- 'user' / 'assistant'
    content        TEXT NOT NULL,
    created_at     TEXT NOT NULL,
    FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE CASCADE
);
```

**关键点：**
- `ON DELETE CASCADE` → 删会话时自动连带删除其消息，不用手动清
- `conversations.db` 文件放在项目根目录（`data/` 下也行，随你定）
- 标题自动生成：新对话取第一句用户问题前 20 字（如"扫地机器人怎么保养"）

### 5.2 侧边栏 UI（app.py）

```
┌─────────────┐
│ 💬 会话记录  │
│ [+ 新建对话] │   ← 按钮：生成新 uuid 并切换到新会话
│─────────────│
│ 📄 扫地机器人 │  ← 历史会话（按更新时间倒序）
│    昨天 14:30 │    [🗑]  ← 删除按钮
│ 📄 怎么保养  │
│    今天 09:12 │    [🗑]
│ 📄 天气查询  │
│    今天 12:40 │    [🗑]   ← 当前会话高亮
└─────────────┘
```

**交互逻辑：**
1. **启动时**：从 `conversations` 表加载全部会话 → 侧边栏渲染；默认进入最近更新的会话
2. **点击会话**：`st.session_state["session_id"]` 切换为该会话 uuid → 从 `messages` 表加载该会话消息 → 页面重渲染
3. **新建对话**：插入新会话记录 → 切换 session_id → 清空消息区
4. **删除**：`DELETE FROM conversations WHERE id=?`（级联删消息）→ 若删的是当前会话，自动切换到最近会话
5. **每轮对话后**：消息写入 `messages` 表，会话 `updated_at` 刷新

### 5.3 Agent 记忆持久化（agent/react_agent.py）

```python
# 改前（内存级，重启即失忆）
self.checkpointer = MemorySaver()

# 改后（SQLite 持久化）
from langgraph.checkpoint.sqlite import SqliteSaver
self.checkpointer = SqliteSaver(conn)   # conn = sqlite3.connect("conversations.db")
```

- `thread_id` 继续用会话 uuid，天然隔离各对话记忆 ✅
- 服务重启后，Agent 能根据 thread_id 找回历史状态，**继续聊天上下文不丢**

### 5.4 依赖

```
# requirements.txt 追加
langgraph-checkpoint-sqlite>=2.0.0
```

（python 内置 `sqlite3`，无需额外安装）

---

## 六、注意事项与风险

| 事项 | 说明 |
|------|------|
| ⚠️ 并发问题 | SQLite 同一时刻单写。当前单人使用没问题；若未来多用户，可加 `check_same_thread=False` + 连接复用，或升级 PostgreSQL |
| ⚠️ 标题乱码风险 | 自动取标题时按 UTF-8 截断，注意别切在半个汉字上（用 Python 切片天然安全） |
| ⚠️ docker 卷 | 部署时 `conversations.db` 必须挂载 volume，否则容器重建数据丢失（compose 已计划加） |
| ⚠️ 旧数据兼容 | 现有 `MemorySaver` 里的历史记忆无法迁移（本来就不持久），改后从零开始，属预期行为 |
| 💡 删除不可逆 | 删除按钮直接物理删除，不提供回收站；如需后悔药可改软删除（加 `deleted_at` 字段） |
| 💡 记忆体积 | 长期使用 messages 表会增长，暂不影响；可定期清理或加"清空全部"按钮 |

---

## 七、实施步骤（审批后执行）

1. `pip install langgraph-checkpoint-sqlite`（装依赖）
2. 新建 `utils/db.py`（建表 + CRUD 函数）
3. 改造 `app.py`（侧边栏 + 切换/新建/删除逻辑 + 消息持久化）
4. 修改 `agent/react_agent.py`（SqliteSaver）
5. 更新 `requirements.txt` / `docker-compose.yml` / `.gitignore`
6. 写 `test/test_session.py` 并跑通
7. 本地 `streamlit run app.py` 实测：
   - 新建 2~3 个会话互发消息 → 切换验证互不干扰
   - 刷新页面 / 重启服务 → 历史仍在
   - 删除会话 → 列表与消息同步消失

---

## 八、效果预览（改造后使用流程）

1. 打开页面 → 左侧列出所有历史会话，右侧进入最近一次对话
2. 点「+ 新建对话」→ 清爽空白页开始新话题
3. 聊到一半想切回早前话题 → 侧边栏点击即回，之前的对话上下文完好
4. 会话太多 → 点 [🗑] 删掉不用的
5. 关电脑重启 → 明天打开，一切还在

---

*此文档供审批，确认后我再动手改代码。*
